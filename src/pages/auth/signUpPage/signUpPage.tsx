import { Page } from "@/components/ui/page";
import { SignUp } from "@/components/auth/signUp";

export const SignUpPage = () => {
  return (
    <Page>
      <SignUp onSubmit={() => {}} />
    </Page>
  );
};
