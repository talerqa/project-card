import { Page } from "@/components/ui/page";
import { SignIn } from "@/components/auth/signIn";

export const SignInPage = () => {
  return (
    <Page>
      <SignIn onSubmit={() => {}} />
    </Page>
  );
};
